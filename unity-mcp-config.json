{
  "mcpServers": {
    "unity": {
      "command": "curl",
      "args": [
        "-X", "POST",
        "-H", "Content-Type: application/json",
        "-d", "@-",
        "http://localhost:5001/"
      ],
      "name": "Unity MCP Server",
      "description": "Direct Unity Editor integration via HTTP"
    }
  }
}